# Minimal Hostinger-friendly security
<IfModule mod_rewrite.c>
RewriteEngine On
# Force HTTPS
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]
</IfModule>

# Basic headers (Hostinger supports mod_headers)
<IfModule mod_headers.c>
  Header always set X-Frame-Options "DENY"
  Header always set X-Content-Type-Options "nosniff"
  Header always set Referrer-Policy "no-referrer"
  # Light CSP to prevent framing and remote scripts; still allow same-origin
  Header always set Content-Security-Policy "default-src 'self'; frame-ancestors 'none'"
</IfModule>

# Block access to hidden files and sensitive configs
<FilesMatch "(?i)^(\.env|\.git|\.htaccess|\.htpasswd|composer\.json|composer\.lock)$">
  Require all denied
</FilesMatch>

# Disable directory listing
Options -Indexes
